import { StyleSheet } from 'aphrodite'

import history from './history'
import circle from './circle'
import info from './info'

export default {
  history: StyleSheet.create(history),
  circle: StyleSheet.create(circle),
  info: StyleSheet.create(info)
}
