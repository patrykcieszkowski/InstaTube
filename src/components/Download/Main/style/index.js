import { StyleSheet } from 'aphrodite'

import timer from './timer'
import grid from './grid'
import media from './media'

export default {
  timer: StyleSheet.create(timer),
  grid: StyleSheet.create(grid),
  media: StyleSheet.create(media)
}
