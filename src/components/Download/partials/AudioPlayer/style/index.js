import { StyleSheet } from 'aphrodite'

import controls from './controls'
import main from './main'
import audio from './audio'

export default {
  main: StyleSheet.create(main),
  controls: StyleSheet.create(controls),
  audio: StyleSheet.create(audio)
}
