import { StyleSheet } from 'aphrodite'

import image from './image'
import fullscreen from './fullscreen'

export default {
  image: StyleSheet.create(image),
  fullscreen: StyleSheet.create(fullscreen)
}
