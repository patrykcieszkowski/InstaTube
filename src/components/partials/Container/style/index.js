import { StyleSheet } from 'aphrodite'

export default StyleSheet.create({
  wrapper: {
    position: `relative`
  },
  noPadding: {
    padding: `0px`
  },
  overflowHidden: {
    overflow: `hidden`
  }
})
