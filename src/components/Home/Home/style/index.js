import { StyleSheet } from 'aphrodite'

import signup from './signup'
import grid from './grid'
import upload from './upload'

export default {
  signup: StyleSheet.create(signup),
  grid: StyleSheet.create(grid),
  upload: StyleSheet.create(upload)
}
